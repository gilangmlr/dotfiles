# Powerlevel10k user config. The theme itself is registered via ZSH_THEME
# in 00-omz.zsh and loaded when oh-my-zsh.sh is sourced in 10-plugins.zsh;
# this file just applies the segment/style overrides.
[[ -r ~/.p10k.zsh ]] && source ~/.p10k.zsh
